---
description: Publish the latest data/ JSON to GitHub Pages
---

1. Run `python -m app.main status` and show me the current counts.
2. Run `python -m app.main sync`.
3. If it reports "Not a git repository", tell me to run `git init`, create a
   **private** GitHub repo, add it as `origin`, and push once manually.
4. Otherwise confirm what was committed and pushed.
